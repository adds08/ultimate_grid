name: ultimate_grid_example
description: "Example app for ultimate_grid — inventory, financial sheet, async paging, merges, search and filters."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  ultimate_grid:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
