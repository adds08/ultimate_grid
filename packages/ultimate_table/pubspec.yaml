name: ultimate_table
description: "Scalable, themable 2D data-grid for Flutter — 9-region freeze, canvas-paint body, merges, async data, selection, sort, filter, search."
version: 0.1.0

# TODO(adds08): set real URLs before publishing. Pub.dev resolves these to
# the "Homepage", "Repository", and "Issue tracker" links on the package
# page. The package itself lives under packages/ultimate_table/ in the
# host monorepo.
# homepage: https://github.com/adds08/flutter_grid_package
# repository: https://github.com/adds08/flutter_grid_package/tree/main/packages/ultimate_table
# issue_tracker: https://github.com/adds08/flutter_grid_package/issues

topics:
  - grid
  - table
  - datagrid
  - spreadsheet
  - timesheet

environment:
  sdk: ^3.9.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
