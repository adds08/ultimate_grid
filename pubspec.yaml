name: ultimate_grid
description: "Scalable, themable 2D data-grid for Flutter — 9-region freeze, canvas-paint body, merges, async data, selection, sort, filter, search."
version: 0.1.0

homepage: https://github.com/adds08/flutter_grid_package
repository: https://github.com/adds08/flutter_grid_package
issue_tracker: https://github.com/adds08/flutter_grid_package/issues

topics:
  - grid
  - table
  - datagrid
  - spreadsheet
  - timesheet

environment:
  sdk: ^3.9.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
